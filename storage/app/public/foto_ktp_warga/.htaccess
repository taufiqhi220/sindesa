# Block direct static file serving by Apache (Force 403 Forbidden for direct HTTP access)
# Laravel PHP handles authorized reads via storage route
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
